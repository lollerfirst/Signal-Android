<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground"
    android:paddingStart="16dp"
    android:paddingTop="10dp"
    android:paddingEnd="16dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/cashu_activity_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginTop="4dp"
        android:contentDescription="@string/app_name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_mobilecoin_avatar_24"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/cashu_activity_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:textAppearance="@style/Signal.Text.Body"
        android:textColor="@color/signal_text_primary"
        app:layout_constraintEnd_toStartOf="@+id/cashu_activity_amount"
        app:layout_constraintStart_toEndOf="@id/cashu_activity_icon"
        app:layout_constraintTop_toTopOf="@id/cashu_activity_icon"
        tools:text="Top-up completed" />

    <TextView
        android:id="@+id/cashu_activity_date"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:textAppearance="@style/Signal.Text.Caption"
        android:textColor="@color/signal_text_secondary"
        app:layout_constraintEnd_toStartOf="@+id/cashu_activity_amount"
        app:layout_constraintStart_toEndOf="@id/cashu_activity_icon"
        app:layout_constraintTop_toBottomOf="@+id/cashu_activity_title"
        tools:text="2/2/20" />

    <TextView
        android:id="@+id/cashu_activity_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/Signal.Text.Body"
        android:textColor="@color/core_green"
        app:layout_constraintBottom_toBottomOf="@+id/cashu_activity_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/cashu_activity_title"
        tools:text="+10,000 sat" />

</androidx.constraintlayout.widget.ConstraintLayout>
