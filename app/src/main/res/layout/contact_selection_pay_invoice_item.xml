<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/rounded_inset_ripple_background"
    android:minHeight="@dimen/contact_selection_item_height"
    tools:viewBindingIgnore="true">

    <!-- Thin separator, spanning 60% width (20% left/right inset) -->
    <View
        android:id="@+id/pay_invoice_separator"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:background="@color/signal_colorSurfaceVariant"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.6" />

    <!-- Centered content row (icon + text) with spacing from separator -->
    <LinearLayout
        android:id="@+id/pay_invoice_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/pay_invoice_separator"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/pay_invoice_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@color/signal_colorSurfaceVariant"
            android:importantForAccessibility="no"
            app:contentPadding="8dp"
            app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.Signal.Circle"
            app:srcCompat="@drawable/symbol_qrcode_24"
            app:tint="@color/signal_colorOnSurface" />

        <TextView
            android:id="@+id/pay_invoice_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:ellipsize="marquee"
            android:labelFor="@id/pay_invoice_icon"
            android:singleLine="true"
            android:text="@string/Payments__pay_invoice"
            android:textAppearance="@style/Signal.Text.BodyLarge" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
